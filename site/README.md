# PolicyGuard marketing site

Static one-pager served at https://policyguard-site.vercel.app

## Deploy

From repo root (Vercel **Root Directory** = `site`):

```bash
cd site
npx vercel --prod
```

Or connect the GitHub repo in Vercel and set Root Directory to `site`.
